{
  "name": "garden-worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply garden-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply garden-db --remote"
  },
  "dependencies": {
    "hono": "^4.6.13",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.86.0"
  }
}
